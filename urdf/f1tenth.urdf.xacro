<?xml version="1.0"?>

<!-- 
This URDF description of the car is taken from the f1tenth_ros_gym package. Modifications
are documented within the xml below.
-->

<!-- A simple model of the racecar for rviz -->

<robot xmlns:xacro="http://www.ros.org/wiki/xacro" name="racecar">

  <xacro:property name="wheelbase" value="0.3302" />
  <xacro:property name="width" value="0.2032" />
  <xacro:property name="height" value="0.1" />
  <xacro:property name="ground_offset" value="0.02" />
  <xacro:property name="wheel_radius" value="0.0508" />
  <xacro:property name="wheel_length" value="0.0381" />
  <xacro:property name="laser_distance_from_base_link" value="0.275" />
  <xacro:property name="laser_height" value="0.05" />
  <xacro:property name="laser_radius" value="0.026" />
  <xacro:property name="car_name" value="ego_racecar" />

  <!-- Mass properties -->
  <xacro:property name="body_mass" value="18.0" />
  <xacro:property name="wheel_mass" value="0.5" />
  <xacro:property name="hinge_mass" value="0.5" />

  <!-- Body inertia: solid box (wheelbase x width x height) at 18kg
       ixx = (1/12)*m*(w^2+h^2) = 1.5*(0.04129+0.01)       = 0.0769
       iyy = (1/12)*m*(l^2+h^2) = 1.5*(0.10903+0.01)       = 0.1786
       izz = (1/12)*m*(l^2+w^2) = 1.5*(0.10903+0.04129)    = 0.2255  -->

  <!-- Wheel inertia: solid cylinder (r=0.0508, h=0.0381) at 0.5kg
       ixx = iyy = (1/12)*m*(3r^2+h^2) = 0.0417*(0.00774+0.00145) = 0.000383
       izz = (1/2)*m*r^2 = 0.25*0.002581                           = 0.000645  -->

  <material name="black">
    <color rgba="0.2 0.2 0.2 1."/>
  </material>

  <material name="blue">
    <color rgba="0.3 0.57 1. 1."/>
  </material>

 <!-- Added collision and inertial tags to support Gazebo physics solver -->
  <link name="${car_name}/base_link">
    <visual>
      <origin xyz="${wheelbase/2} 0 ${ground_offset+height/2}"/>
      <geometry>
        <box size="${wheelbase} ${width} ${height}"/>
      </geometry>
      <material name="blue"/>
    </visual>

    <collision>
      <origin xyz="${wheelbase/2} 0 ${ground_offset+height/2}"/>
      <geometry>
        <box size="${wheelbase * 0.8} ${width * 0.8} ${height}"/>
      </geometry>
    </collision>
    
    <inertial>
      <origin xyz="${wheelbase/2} 0 ${ground_offset+height/2}"/>
      <mass value="${body_mass}"/>
      <inertia ixx="0.0769" ixy="0.0" ixz="0.0" 
               iyy="0.1786" iyz="0.0" 
               izz="0.2255"/>
    </inertial>
  </link>

  <!-- Render color for base link on Gazebo -->
  <gazebo reference="${car_name}/base_link">
    <material>Gazebo/Blue</material>
  </gazebo>

  <joint name="base_to_laser_model" type="fixed">
    <parent link="${car_name}/base_link"/>
    <child link="${car_name}/laser_model"/>
    <origin xyz="${laser_distance_from_base_link} 0 ${ground_offset+height+(laser_height/2)}"/>
  </joint>

  <link name="${car_name}/laser_model">
    <visual>
      <geometry>
        <cylinder radius="${laser_radius}" length="${laser_height}"/>
      </geometry>
      <material name="black"/>
    </visual>
  </link>

  <!-- Continuous joint to allow wheel to rotate 360 degrees -->
  <joint name="base_to_back_left_wheel" type="continuous">
    <parent link="${car_name}/base_link"/>
    <child link="${car_name}/back_left_wheel"/>
    <origin xyz="0 ${(wheel_length+width)/2} ${wheel_radius}"/>
    <axis xyz="0 1 0"/>
    <limit effort="100.0" velocity="100.0"/>
    <dynamics damping="0.01" friction="0.0"/>
  </joint>

  <!-- Added collision and inertial tags to support Gazebo physics solver -->
  <link name="${car_name}/back_left_wheel">
    <visual>
      <geometry>
        <cylinder radius="${wheel_radius}" length="${wheel_length}"/>
      </geometry>
      <material name="black"/>
      <origin rpy="${pi/2} 0 0"/>
    </visual>

    <collision>
      <origin rpy="${pi/2} 0 0"/>
      <geometry>
        <cylinder radius="${wheel_radius}" length="${wheel_length}"/>
      </geometry>
    </collision>
    
    <inertial>
      <origin rpy="${pi/2} 0 0"/>
      <mass value="${wheel_mass}"/>
      <inertia ixx="0.000383" ixy="0.0" ixz="0.0" 
               iyy="0.000383" iyz="0.0" 
               izz="0.000645"/>
    </inertial>
  </link>

  <!-- Render color for back left wheel on Gazebo -->
  <gazebo reference="${car_name}/back_left_wheel">
    <material>Gazebo/Black</material>
    <mu1>1.5</mu1>
    <mu2>2.5</mu2>
    <kp>500000.0</kp> 
    <kd>10.0</kd>
    <minDepth>0.001</minDepth>
  </gazebo>

  <!-- Continuous join to allow wheel to rotate 360 degrees -->
  <joint name="base_to_back_right_wheel" type="continuous">
    <parent link="${car_name}/base_link"/>
    <child link="${car_name}/back_right_wheel"/>
    <origin xyz="0 ${-(wheel_length+width)/2} ${wheel_radius}"/>
    <axis xyz="0 1 0"/>
    <limit effort="100.0" velocity="100.0"/>
    <dynamics damping="0.01" friction="0.0"/>
  </joint>

  <!-- Added collision and inertial tags to support Gazebo physics solver -->
  <link name="${car_name}/back_right_wheel">
    <visual>
      <geometry>
        <cylinder radius="${wheel_radius}" length="${wheel_length}"/>
      </geometry>
      <material name="black"/>
      <origin rpy="${pi/2} 0 0"/>
    </visual>

    <collision>
      <origin rpy="${pi/2} 0 0"/>
      <geometry>
        <cylinder radius="${wheel_radius}" length="${wheel_length}"/>
      </geometry>
    </collision>
    
    <inertial>
      <origin rpy="${pi/2} 0 0"/>
      <mass value="${wheel_mass}"/>
      <inertia ixx="0.000383" ixy="0.0" ixz="0.0" 
               iyy="0.000383" iyz="0.0" 
               izz="0.000645"/>
    </inertial>
  </link>

  <!-- Render color for back right wheel on Gazebo -->
  <gazebo reference="${car_name}/back_right_wheel">
    <material>Gazebo/Black</material>
    <mu1>1.5</mu1>
    <mu2>2.5</mu2>
    <kp>500000.0</kp> 
    <kd>10.0</kd>
    <minDepth>0.001</minDepth>
  </gazebo>

  <!-- Revolute joint to allow wheel axel to rotate along Z axis -->
  <joint name="base_to_front_left_hinge" type="revolute">
    <parent link="${car_name}/base_link"/>
    <child link="${car_name}/front_left_hinge"/>
    <origin xyz="${wheelbase} ${(wheel_length+width)/2} ${wheel_radius}"/>
    <axis xyz="0 0 1"/>
    <limit lower="-0.5" upper="0.5" effort="10.0" velocity="5.0"/>
    <dynamics damping="0.1" friction="0.0"/>
  </joint>

  <link name="${car_name}/front_left_hinge">
    <inertial>
      <mass value="${hinge_mass}"/>
      <inertia ixx="0.001" ixy="0.0" ixz="0.0" 
               iyy="0.001" iyz="0.0" 
               izz="0.001"/>
    </inertial>
  </link>

  <!-- Continuous joint to allow wheel to rotate 360 degrees -->
  <joint name="front_left_hinge_to_wheel" type="continuous">
    <parent link="${car_name}/front_left_hinge"/>
    <child link="${car_name}/front_left_wheel"/>
    <axis xyz="0 1 0"/>
    <limit effort="100.0" velocity="100.0"/>
    <dynamics damping="0.01" friction="0.0"/>
  </joint>

  <!-- Added collision and inertial tags to support Gazebo physics solver -->
  <link name="${car_name}/front_left_wheel">
    <visual>
      <geometry>
        <cylinder radius="${wheel_radius}" length="${wheel_length}"/>
      </geometry>
      <material name="black"/>
      <origin rpy="${pi/2} 0 0"/>
    </visual>

    <collision>
      <origin rpy="${pi/2} 0 0"/>
      <geometry>
        <cylinder radius="${wheel_radius}" length="${wheel_length}"/>
      </geometry>
    </collision>
    
    <inertial>
      <origin rpy="${pi/2} 0 0"/>
      <mass value="${wheel_mass}"/>
      <inertia ixx="0.000383" ixy="0.0" ixz="0.0" 
               iyy="0.000383" iyz="0.0" 
               izz="0.000645"/>
    </inertial>
  </link>

  <!-- Render color for front left wheel on Gazebo -->
  <gazebo reference="${car_name}/front_left_wheel">
    <material>Gazebo/Black</material>
    <mu1>1.5</mu1>
    <mu2>2.5</mu2>
    <kp>500000.0</kp> 
    <kd>10.0</kd>
    <minDepth>0.001</minDepth>
  </gazebo>

  <!-- Revolute joint to allow wheel to spin 360 degrees -->
  <joint name="base_to_front_right_hinge" type="revolute">
    <parent link="${car_name}/base_link"/>
    <child link="${car_name}/front_right_hinge"/>
    <origin xyz="${wheelbase} ${-(wheel_length+width)/2} ${wheel_radius}"/>
    <axis xyz="0 0 1"/>
    <limit lower="-0.5" upper="0.5" effort="10.0" velocity="5.0"/>
    <dynamics damping="0.1" friction="0.0"/>
  </joint>

  <link name="${car_name}/front_right_hinge">
    <inertial>
      <mass value="${hinge_mass}"/>
      <inertia ixx="0.001" ixy="0.0" ixz="0.0" 
               iyy="0.001" iyz="0.0" 
               izz="0.001"/>
    </inertial>
  </link>

  <!-- Continuous joint to allow wheel to rotate 360 degrees -->
  <joint name="front_right_hinge_to_wheel" type="continuous">
    <parent link="${car_name}/front_right_hinge"/>
    <child link="${car_name}/front_right_wheel"/>
    <axis xyz="0 1 0"/>
    <limit effort="100.0" velocity="100.0"/>
    <dynamics damping="0.01" friction="0.0"/>
  </joint>

  <!-- Added collision and inertial tags to support Gazebo physics solver -->
  <link name="${car_name}/front_right_wheel">
    <visual>
      <geometry>
        <cylinder radius="${wheel_radius}" length="${wheel_length}"/>
      </geometry>
      <material name="black"/>
      <origin rpy="${pi/2} 0 0"/>
    </visual>

    <collision>
      <origin rpy="${pi/2} 0 0"/>
      <geometry>
        <cylinder radius="${wheel_radius}" length="${wheel_length}"/>
      </geometry>
    </collision>
    
    <inertial>
      <origin rpy="${pi/2} 0 0"/>
      <mass value="${wheel_mass}"/>
      <inertia ixx="0.000383" ixy="0.0" ixz="0.0" 
               iyy="0.000383" iyz="0.0" 
               izz="0.000645"/>
    </inertial>
  </link>

  <!-- Render color for front right wheel on Gazebo -->
  <gazebo reference="${car_name}/front_right_wheel">
    <material>Gazebo/Black</material>
    <mu1>1.5</mu1>
    <mu2>2.5</mu2>
    <kp>500000.0</kp> 
    <kd>10.0</kd>
    <minDepth>0.001</minDepth>
  </gazebo>

  <!-- Include URDF description for camera -->
  <xacro:include filename="$(find realsense2_description)/urdf/_d435i.urdf.xacro" />

  <xacro:sensor_d435i parent="${car_name}/base_link" name="camera">
    <origin xyz="0.35 0.0 0.15" rpy="0 0 0"/> 
  </xacro:sensor_d435i>

  <gazebo reference="camera_link">
    <sensor type="depth" name="camera">
      <always_on>true</always_on>
      <update_rate>30.0</update_rate>
      <camera name="camera">
        <horizontal_fov>1.518</horizontal_fov>
        <image>
          <width>640</width>
          <height>480</height>
          <format>R8G8B8</format>
        </image>
        <clip>
          <near>0.1</near>
          <far>10.0</far>
        </clip>
      </camera>
      
      <plugin name="camera_plugin" filename="libgazebo_ros_camera.so">
        <ros>
          <namespace>/</namespace>
        </ros>
        <camera_name>camera</camera_name>
        <image_topic_name>color/image_raw</image_topic_name>
        <camera_info_topic_name>color/camera_info</camera_info_topic_name>
        <depth_image_topic_name>depth/image_rect_raw</depth_image_topic_name>
        <depth_image_camera_info_topic_name>depth/camera_info</depth_image_camera_info_topic_name>
        <point_cloud_topic_name>points</point_cloud_topic_name>
        <frame_name>camera_depth_optical_frame</frame_name>
        <min_depth>0.1</min_depth>
        <max_depth>10.0</max_depth>
      </plugin>
    </sensor>
  </gazebo>

  <gazebo>
    <plugin name="gazebo_ros_ackermann_drive" filename="libgazebo_ros_ackermann_drive.so">
      <ros>
        <remapping>cmd_vel:=cmd_vel</remapping>
        <remapping>odom:=odom</remapping>
      </ros>
      <update_rate>50.0</update_rate>

      <left_steering_joint>base_to_front_left_hinge</left_steering_joint>
      <right_steering_joint>base_to_front_right_hinge</right_steering_joint>

      <rear_left_joint>base_to_back_left_wheel</rear_left_joint>
      <rear_right_joint>base_to_back_right_wheel</rear_right_joint>
      
      <front_left_joint>front_left_hinge_to_wheel</front_left_joint>
      <front_right_joint>front_right_hinge_to_wheel</front_right_joint>

      <wheel_separation>${width + wheel_length}</wheel_separation>
      <wheel_base>${wheelbase}</wheel_base>

      <wheel_radius>${wheel_radius}</wheel_radius>
      <wheel_diameter>${wheel_radius * 2}</wheel_diameter>
      
      <max_steer>0.5</max_steer>
      <max_speed>5.0</max_speed>
      <max_wheel_torque>100.0</max_wheel_torque>

      <linear_velocity_pid_gain>5.0 0.5 0.05</linear_velocity_pid_gain>
      <left_steering_pid_gain>25.0 2.0 0.3</left_steering_pid_gain>
      <right_steering_pid_gain>25.0 2.0 0.3</right_steering_pid_gain>

      <publish_odom>true</publish_odom>
      <publish_odom_tf>true</publish_odom_tf>
      <publish_wheel_tf>true</publish_wheel_tf>
      <odometry_frame>odom</odometry_frame>
      <robot_base_frame>${car_name}/base_link</robot_base_frame>
    </plugin>
  </gazebo>

</robot>
